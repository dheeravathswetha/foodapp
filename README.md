
  # Add Prototypes to Login

  This is a code bundle for Add Prototypes to Login. The original project is available at https://www.figma.com/design/ATpAjMHyQUkNsc9fQ2TXKJ/Add-Prototypes-to-Login.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  